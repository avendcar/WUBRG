import 'package:flutter/material.dart';

class UserTile extends StatelessWidget {
  const UserTile({super.key});
  //Tiles that will be displayed in the main menu representing other users. Will soon replace the existing generic buttons(tiles).
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}