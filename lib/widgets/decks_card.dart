import 'package:flutter/material.dart';

class DecksCard extends StatelessWidget {
  const DecksCard({super.key});

  @override
  Widget build(BuildContext context) {
    //TODO: Implement deck card that takes deck tiles and is displayed on the profile
    return Container();
  }
}