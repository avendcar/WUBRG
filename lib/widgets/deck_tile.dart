import 'package:flutter/material.dart';

class DeckTile extends StatelessWidget {
  const DeckTile({super.key});

  @override
  Widget build(BuildContext context) {
    //TODO: Implement deck tiles to be displayed in decks_card.dart
    return Container();
  }
}